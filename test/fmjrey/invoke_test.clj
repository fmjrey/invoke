(ns fmjrey.invoke-test
  (:require [clojure.test :refer [deftest is testing]]
            [fmjrey.invoke :as sut])) ; system under test

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
